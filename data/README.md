# Data

Runtime-facing metadata lives here.

Planned subfolders:
- `anchors/` target hotspots and descriptive metadata
- `manifests/` asset manifests for hero images, tiles, zoom paths, and mode boundaries

Do not put raw Gaia full datasets here.
Store only runtime-ready subsets and manifests.
